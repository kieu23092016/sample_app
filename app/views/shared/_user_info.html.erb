<%= link_to gravatar_for(current_user, size: Settings.size.gravatar), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("text.view_profile"), current_user %></span>
<span><%= pluralize(current_user.microposts.count, t("text.micropost")) %></span>
